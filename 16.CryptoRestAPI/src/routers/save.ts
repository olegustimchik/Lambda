import { MarketFactory } from "../markets/market-factory.ts";
import express from "express";
const saveRouter = express.Router();

saveRouter.get("/save", (reg, res) => {

});

export { saveRouter }; 